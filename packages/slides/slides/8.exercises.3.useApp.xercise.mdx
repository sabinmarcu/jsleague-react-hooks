<!-- SUBTITLE: useApp -->

<Layout header>

# Whole App

</Layout>

---

<Layout>

<div style={{ maxWidth: 1000 }}>

In this exercise, we will be creating a `useApp` functionality, which will leverage our `useField` hook and context + `useContext` to abstract the data layer, and offer the fields to the **Editor Component**, and the list of values to our **Preview** component.

</div>

</Layout>

---

<Layout>

This is how the full app should look like:

<style>{`
  .MuiCardContent-root > div {
    font-size: initial;
  }
`}</style>

<div>

<App />

</div>

</Layout>